apiVersion: v1
kind: Service
metadata:
  name: node-exporter
  namespace: monitoring
  labels:
    app.kubernetes.io/name: node-exporter
    app.kubernetes.io/part-of: monitoring
spec:
  clusterIP: None
  ports:
    - port: 9100
      targetPort: 9100
  selector:
    app.kubernetes.io/name: node-exporter
